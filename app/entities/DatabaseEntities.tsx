export interface MoodJournalEntry {
    id: number; // Unique identifier for the entry
    mood: string; // Mood description (e.g., "Happy", "Sad", etc.)
    notes: string; // Additional notes or reflections
}